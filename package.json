{
  "name": "vitinn",
  "version": "0.1.0",
  "description": "Personal site for the dissemination of my work.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "npm install --production && node src/server",
    "dev": "set NODE_ENV=development && node --trace-warnings --watch src/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vitor Lopes Franco",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "ejs": "^3.1.8",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.34.0"
  }
}
